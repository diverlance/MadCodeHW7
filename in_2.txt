	int i =3;
	if(i<0)
	{
		i = 0;
	}
	string filename = "html_page.html";
	file webpage = load(filename); //load a html file
	table t = getTable(webpage); //load the first seen table from webpage
	table_data td = t.getData(1, 2); //get data in row 1 and column 2 in t
	t.print(); //print table
	td.print(); //print table data
	t.setData(1,2,"12345"); //set the data at (1,2) to "12345"
	close(webpage); //close webpage file